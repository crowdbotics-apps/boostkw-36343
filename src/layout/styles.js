import { StyleSheet } from "react-native"

import * as appStyles from "../util/appStyles"

export const styles = StyleSheet.create({
  contentGradient: {
    position: "absolute",
    top: 0,
    bottom: 0,
    left: 0,
    right: 0
  },
  // logo: {
  //   marginTop: appStyles.hp(5),
  //   alignItems: "center"
  // }
})
